#!/bin/sh
#-----------------------------------------------------------------------------
# Usage: tVLATapeInput.exec
#-----------------------------------------------------------------------------
# This script executes the program tVLATapeInput to test if new the
# measurement set concatrenation is working.
# $Id$
#-----------------------------------------------------------------------------

  if [ "$CASAPATH" = "" ]
  then
     echo "UNTESTED: tVLALogicalRecord (CASAPATH not defined)"
     exit 3
  fi
  echo "UNTESTED"
  exit 3
