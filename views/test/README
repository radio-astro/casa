This view exists to simplify the checkout of just the directories required to build 
and test the CASA components:

   * casacore
   * code
   * asap

from the _test_ branch.

Do NOT 'svn add' any files or directories to the directory containing this README file!
